_schema-version: 3.3.0
ID: ZRAMA_PLANNED_ORDER
version: 1.0.0
description: "Test Planned Order Application"
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: ZRAMA_PLANNED_ORDER-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: ZRAMA_PLANNED_ORDER-auth
      - name: ZRAMA_PLANNED_ORDER-db

  - name: ZRAMA_PLANNED_ORDER-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: ZRAMA_PLANNED_ORDER-db

resources:
  - name: ZRAMA_PLANNED_ORDER-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: ZRAMA_PLANNED_ORDER-${org}-${space}
        tenant-mode: dedicated
  - name: ZRAMA_PLANNED_ORDER-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
